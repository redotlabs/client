import { PATH } from './routes';

export const AUTH_WHITE_LIST = [
  PATH.root,
  PATH.terms,
  PATH.privacy,
  PATH.pricing,
  PATH.consultation.new,
  PATH.consultation.renewal,
  PATH.auth.signIn,
  PATH.auth.signUp,
];
