---
import "../styles/global.css";
import ThemeRoot from "../components/theme-root";
import { Logo, Button } from '@redotlabs/ui';


const navItems = [
  { href: '/', label: '리닷 소개' },
  { href: '/logo', label: '리닷 브랜드 로고' },
  { href: '/assets', label: '리닷 브랜드 에셋' }
];

const { title = 'redot BRAND' } = Astro.props;
const currentPath = Astro.url.pathname;
---
<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="icon" href="/logo.svg" />
  </head>
  <body>
    <ThemeRoot client:load>
    <div class="flex min-h-screen flex-col">
      <header class="sticky top-0 z-10 flex items-center justify-between border-b border-(--border) px-20 py-2.25 max-md:px-5 max-md:py-4">
        <div class="flex items-center gap-2">
           <Logo client:load className="w-auto h-6" />
          <span class="text-[22px] leading-normal font-semibold tracking-[-0.02em]">BRAND</span>
        </div>
        <div class="flex items-center gap-3">
          <Button
            className="border border-(--border) px-6 py-2 font-semibold"
            variant="contained"
            data-theme-toggle
            client:idle
          >
            다크모드
          </Button>
          <a href="https://redot.me" target="_blank" rel="noreferrer">
            <Button
              className="px-[22px] py-[10px] font-semibold text-white"
              client:idle
            >
              redot 사이트로 이동
            </Button>
          </a>
        </div>
      </header>

      <main class="grid grid-cols-[240px_minmax(0,1fr)] gap-8 px-12 pb-20 pt-10 max-lg:grid-cols-1 max-lg:gap-6 max-md:px-5 max-md:pb-16">
        <aside class="flex flex-col gap-5 max-lg:flex-row max-lg:items-center max-lg:justify-between">
          <div class="flex items-center gap-3 font-[var(--font-display)] text-[32px] font-bold">
            <img src="/logo.svg" width="44" height="44" alt="redot" />
            <span>redot</span>
          </div>
          <nav class="flex flex-col gap-3 font-semibold text-[var(--text-secondary)] max-lg:flex-row max-lg:flex-wrap">
            {navItems.map((item) => (
              <a
                href={item.href}
                class:list={[
                  'rounded-lg px-1 py-2 transition-colors',
                  currentPath === item.href && 'bg-[var(--surface-soft)] text-[var(--brand-blue)]'
                ]}
              >
                {item.label}
              </a>
            ))}
          </nav>
        </aside>
        <section class="max-w-[1040px]">
          <slot />
        </section>
      </main>

      <footer class="mt-auto bg-[var(--brand-blue)] text-[#e8efff]">
        <div class="flex flex-wrap justify-between gap-6 px-12 pb-10 pt-7 text-[13px] max-md:px-5">
          <div>
            <div class="flex items-center gap-2 font-semibold">
              <img src="/logo.svg" width="20" height="20" alt="redot" />
              redot
            </div>
            <div class="mt-3 leading-relaxed">
              <div>사업자등록번호: 123-45-67890 | 대표: 박도윤</div>
              <div>서울특별시 송파구 송파대로 14-18, 4층</div>
              <div>연락처: contact@redot.me</div>
            </div>
          </div>
          <div class="text-right">
            <div class="flex items-center justify-end gap-3 text-[12px]">
              <a href="/terms">이용약관</a>
              <span>|</span>
              <a href="/privacy">개인정보처리방침</a>
            </div>
            <div class="mt-3 text-[12px]">©2025 redot. All rights reserved.</div>
          </div>
        </div>
      </footer>
    </div>
    </ThemeRoot>

    <script is:inline>
      const root = document.documentElement;
      const saved = localStorage.getItem('theme');
      const prefersDark = window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches;
      const initialTheme = saved || (prefersDark ? 'dark' : 'light');
      root.dataset.theme = initialTheme;

      const button = document.querySelector('[data-theme-toggle]');
      const updateLabel = (theme) => {
        if (!button) return;
        button.setAttribute('aria-pressed', theme === 'dark');
        button.textContent = theme === 'dark' ? '라이트모드' : '다크모드';
      };

      updateLabel(initialTheme);
      button?.addEventListener('click', () => {
        const nextTheme = root.dataset.theme === 'dark' ? 'light' : 'dark';
        root.dataset.theme = nextTheme;
        localStorage.setItem('theme', nextTheme);
        updateLabel(nextTheme);
      });
    </script>
  </body>
</html>
